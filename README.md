# Assistable Pricing Sheet

Interactive pricing page. To run locally:
1. npm install
2. npm run dev
Then open the link it shows (usually http://localhost:5173).

Deployed on Vercel — pushing to GitHub auto-updates the live site.
